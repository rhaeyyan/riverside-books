# npm scripts the docs mention on purpose, before any package.json defines them.
#
# Checked by .github/scripts/check-doc-commands.py. Adding a name here says "this
# doc is knowingly describing future state" — so keep the reason current, and
# delete the line once the script actually exists.

# The markdown lint/format toolchain. .markdownlint-cli2.jsonc and .prettierrc.json
# are both committed at the repo root, but there is no root package.json to hang the
# scripts on yet, so nothing runs them. Referenced by CLAUDE.md, AGENTS.md,
# CONTRIBUTING.md, and .github/PULL_REQUEST_TEMPLATE.md.
lint:md
format:md
format:md:check
